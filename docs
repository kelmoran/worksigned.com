
Docs
MUST be professional and extensive, use siomple english no corny or non technical stuff (not overly technical either)


should have every app availabe in new tab

    should have every function implemented for each app - LONG list for libreoffice - but list then - take them from libreoffice docs

should be divided into sections - section links on left - make it official documentation type style page


main doc should describe in a technical tone of how it works

can operate in multiple modes - worksigned/bring your own private cloud, browser, local; public/private llms, that we have our own dedicated llms


should discuss technical details of proof chain
    how its constructed - a combination ofa  git style file repo plus user actions - files as starting point, then clicks and keypresses mutate the state
    verify the state after each files/app closed

    give examples - we also store llm and human email/chat interactions

    describe convincingly how accounting and events are inserted into the ledger (append only)




auditing
    users should be able to audit the Worksigned servers - the data servers are server b
        they should be able to send a prebaked command eg via web gui to the servers, listing all their processes eg ps aux and even iotop and kernel stuff ntop to see traffic pertaining to their user, as well as shared and root daemons 

        this is done at an interval for transparency

        they can also come and audit in person by appointment

        wire this up as an inline demo

        they should be able to read their logs - grep their user to avoid privacy issues

        they should be able to see the flow of their data and their queries - network hops and machines, and where it was encrypted, where the tunnel exits are, who in the middle is an isp or whatever




